import { expect } from 'chai';

import { Player } from './player.js';

describe('Player', () => {
  it('should play numbers sequentially', () => {
    const player = new Player();
    expect(player.playFrom(['yesterday', 'money'])).to.equal('play yesterday, play money');
  });
});