export class Player {
  constructor() {
  }
  /**
   * 
   * @param {string[]} playlist 
   * @returns string
   */
  playFrom(playlist) {
    return playlist.map((name) => `play ${name}`).join(', ');
  }
}