{
  "name": "typescript-jsdoc-sandbox",
  "private": true,
  "scripts": {
    "workspace:install": "cd node_modules/typed-lib && npm install",
    "test": "cd node_modules/typed-lib && npm test"
  }
}
